# 交付状态与版本口径

## 2026-09-26 独立证据交接实现

在已核验整合分支 `0c0bca15...` 基础上，按用户批准的产品完善计划独立增加 `security/export_evidence.py`、`security/demo_evidence.py` 与 `tests/security/test_evidence_bundle.py`。保存真实请求、程序身份和完整操作结果，核对源码引用并支持另一进程复核；不改变关系求解、不自动汇总安全判断。

这不是原始候选文件或其历史验证的恢复。与将来取得的原包仍须逐文件比较，不能用同名新文件、专项数量或当前成功回放满足原候选身份验收。原有发布门禁和 DCO 不变；本批实现不代表 main 已合并或预览版已发布。调用和限制见[离线证据交接](../reference/evidence-handoff.md)。

## 2026-09-21 整合检查基线（历史记录）

本页记录本次检查时的状态，不把源码中的版本字符串当作已发布版本。最终是否合并、发布，应分别核对 PR 的 merged 字段、远程 main、标签目标和 Release 附件。

| 对象 | 已核对状态 |
|---|---|
| `main` | `bec1cd1ca1a9b0fd95fd5340b55d80bc61bb743c`，主线程序为 `0.14.0-dev` |
| PR #5 的本轮起点 | `46d345917cc4ed06a4152ed9e6ccc1b7fd4c54f7`，三个提交，未合并草稿 |
| 整合分支 | `release/prepare-cbm-sec-v0.15.0-preview.1`，程序内部版本为 `0.15.0-preview.1`，注册十二个只读 MCP 工具 |
| `cbm-sec-v0.6.0-preview.1` | 历史预览发布，不包含后续开发能力，不移动或覆盖 |
| `cbm-sec-v0.15.0-preview.1` | 预定标签，检查基线时尚不存在，不是已经发布的版本 |

## 已在整合分支的内容

普通 Java 实参回溯、普通 MyBatis 资源操作清单、单操作完整结果缓存的核心代码已经进入分支。旧 MyBatis 专用回溯继续保留。这不等于附件中的完整候选已经整合。

本轮将已知的协议数量、缓存测试、示例、工具表、技能和版本说明集中对齐。仅修正现有接口的兼容和说明；不增加分析范围，不建设五阶段运行系统，不拆分分析核心或改动构建和发行通道。

## 尚未取得的完整候选

原始源码包与验证记录在文件库可见，但本轮没有取得其授权原始字节。以下身份仍是交接期望值，尚未完成本轮文件哈希核验：

- 源码包 `cbm-sec-argument-origins-source.tar.gz`，SHA-256 `2c1b8caac79dfb371b87932e60963bc2211f5c364f73f0a8b28c3a6c833940f6`。
- 记录 `cbm-sec-argument-origins-validation.json`，SHA-256 `486c38815611e50053df097197efc0c00f168b0208fc661597ec71076024e313`。
- 候选 Git 树 `7aea3ca69f8e660bfb6adf79c28c3a1ec80fcdbc`，内部版本 `0.15.0-dev`。

本轮没有把索引文本、参考方案或当前远程中间代码重新包装成这个候选。缺失内容必须从经过身份核验的原始候选整合，不能用一份同名占位文件代替。

| 待整合内容 | 状态 |
|---|---|
| `tests/security/test_argument_origins.py`、`security/demo_arguments.py` | 原始候选专项及回放未同步 |
| 资源操作和操作缓存的新增专项与示例 | 原始候选完整测试集未同步，已有兼容测试不替代它们 |
| 原候选 `security/export_evidence.py`、导出测试及示例 | 原件未恢复；本批独立实现见上，不合并计算历史测试 |
| `tests/java-argument-reference/` | 原始受控 Java 参考未同步 |
| 完整候选参考文档和技能附件 | 当前接口说明已做兼容修正，仍需与原包逐项比较 |

范围扫描、检查点和历史 CFG/finally 实验不在本次整合范围。

## 验证与合并要求

历史 1,156 项专项测试对应 `0.15.0-dev` 候选，不是当前分支或未来发布提交的新测试结果。本轮静态语法/差异检查与远程产品运行测试分开报告。完整安全回归、GCC、Clang 内存检查及受控 Java 对照只有实际完成后才能计为通过。

本轮起点的 Security facts 未完成全部专项；DCO 和总 PR 检查存在失败。DCO 由具备资格的贡献者完成真实声明，不代签、关闭检查或以管理员权限绕过。修改后的 CI 结果应查看对应 head，不能沿用起点记录。

拿到完整候选后，按共同基线逐文件整合，保留分支已有工作；对最终代码运行文档、构建和完整专项验证，经正常 PR 合入 main。不能用整目录覆盖或强制推送代替整合。

## 发布要求

新版只使用新的独立 CBM Sec 预览标签。固定合并后的实际提交，重新构建并测试程序；先创建草稿，上传源码、已测平台程序、校验清单和真实验证记录，回读校验后再发布 prerelease。当前没有新版发布产物可供使用。

完整原版 CBM 回归、其他平台、真实 Codex/pi、业务漏洞召回与模型成本均不因安全专项成功而自动得到验证。保留上游原文、许可证、历史标签和已有附件，不修改其他仓库、客户端或 Hooks。
