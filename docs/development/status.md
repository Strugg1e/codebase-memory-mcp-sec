# 交付状态与版本口径

本次主分支整合以安全开发提交 `e3b2716931caa634dd8ea850f79ffa2e64a6c6c1` 和目录整理提交 `924030ae11952021562566f39f0a999b04f2b7c2` 为基础。程序 `SF_VERSION` 仍为 `0.14.0-dev`，包含受类型约束的 Java String 返回模型和十个 MCP 工具。合并不升级分析器，也不发布新的稳定版。

## 分支与交付物

| 位置 | 状态 |
|---|---|
| 默认 `main` | 统一后的安全开发入口，包含安全源码、中文首页、默认安全构建和目录整理 |
| `feat/security-facts-v0.1` | 本次安全功能的来源分支；新接入默认使用 `main` |
| `cbm-sec-v0.6.0-preview.1` | 历史发布标签和预览包，未移动、未覆盖，不包含后续开发功能 |
| 范围危险点发现、检查点恢复 | 先前提供的本地候选，本次未合入 |
| 资源操作上下文 Preview 1 | 单独的本地候选，本次未合入 |

2026-09-14，仓库所有者明确要求合并到 `main`。整合顺序是：首页 PR #2 进入 `main`；目录 PR #3 进入安全开发分支；解决首页冲突后，通过 PR #1 将安全功能与目录统一到 `main`。实际完成状态以各 PR 的 merged 字段和远程 main 提交为准。

首页冲突保留更完整的目录整理版，同时保留上游 README 原始归档。没有把尚未提交的本地功能候选混入合并，没有删除来源分支或改写历史提交。

## 验证口径

合并前核对：目录整理提交的 Security facts、Spring 入口、MyBatis 模板、Spring Security 控制参考任务均为成功。相关历史提交仍存在 DCO 与上游 PR 测试失败记录，不能把本次合并解释成整仓检查全绿。

本次合并没有关闭检查、修改保护规则、伪造贡献署名或使用强制推送。冲突处理只涉及首页和交付状态文档，不改变分析代码。最终整合提交的验证状态与既有提交的验证记录应分别查看。

同名版本不能代替提交、构建指纹和能力表。本地补丁应先核对基线，不在已叠加其他候选的目录里强制应用。后续候选功能需逐项协调、验证后再合入；新 Release 另行发布。
