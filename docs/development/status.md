# 交付状态与版本口径

本次核对的远程开发基线：`e3b2716931caa634dd8ea850f79ffa2e64a6c6c1`。程序 `SF_VERSION` 为 `0.14.0-dev`；在原 v0.14 之上，已经有受类型约束的 Java String 返回模型。PR 标题或早期摘要可能落后于实际代码。

| 位置 | 状态 |
|---|---|
| 默认 `main` | 安全代码尚未合入的上游基线；首页文案需独立修订 |
| `feat/security-facts-v0.1` | 本文核对的安全开发线，十个 MCP 工具 |
| `cbm-sec-v0.6.0-preview.1` | 历史发布标签，不因开发分支推进而更新 |
| 范围危险点发现、检查点恢复 | 先前提供的本地候选，本轮没有并入 |
| 资源操作上下文 Preview 1 | 单独基于 `e3b2716` 的本地候选，本轮没有并入 |
| 本次目录整理 | 基于已存在的开发源码，不宣称增加分析能力 |

同名版本不能代替提交、构建指纹和能力表。本地补丁应先核对基线，不在已叠加其他候选的目录里强制应用。

本次分别准备默认分支首页 PR 和开发目录 PR。首页 PR 只更新中文介绍及上游原文归档，不隐式合入实验代码；目录 PR 以安全开发线为基础。任何 PR 合并前，默认首页都不会自动改变。

后续应把经过验证的功能候选逐项协调到同一开发线，再决定新的预览发行版；不在 README 中提前宣称候选功能已发布。
