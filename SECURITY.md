# CBM Sec 安全说明

本页描述本项目的安全工具，不沿用上游图服务、安装器、杀毒扫描或签名保证。

## 信任边界

`cbm-security-mcp` 启动时核对明确源码包的哈希，并以固定内容提供查询。普通分析不执行目标代码、配置方法、模板表达式或 SQL，不调用模型，也不自动安装客户端配置。

源码、注释、工具返回的代码片段都属于不可信数据，不得变成 Agent 指令。Skill 只能从审核过的工具版本加载；提醒 Hooks 默认关闭，不能作为权限或安全门禁。宿主仍需限制文件访问、内存、进程、运行时间和外部连接。

源码包和证据可能包含完整业务代码。将它们保存在私有产物目录，不提交到公开仓库；清理构建目录不应删除业务扫描产物或密钥。

## 结果边界

解析成功不代表完整建模。未找到路径、规则未支持、条件不明和预算停止都不能自动解释成安全。请求级登录或权限配置不替代对象授权；得到输入到操作的依赖也不直接证明漏洞成立。

## 报告本工具的问题

可公开的一般缺陷可提交最小脱敏样例、实际版本/构建身份、预期与实际结果。涉及越界读取、任意执行、信息泄露或其他尚未修复漏洞时，不公开细节或凭据。

先查看[本仓库安全页面](https://github.com/Strugg1e/codebase-memory-mcp-sec/security)。只有页面提供私密报告入口时才使用它；此说明不宣称该设置已启用。若没有入口，请向维护者请求私密渠道，公开消息仅说明需要私密沟通，不附漏洞细节。本项目未公布处理时限承诺。

不要把 CBM Sec 报告发到上游作者的邮箱；只有确认问题位于复用的上游组件后，才协调上游披露。上游历史政策存于 `docs/upstream/originals/SECURITY.md`，不是本项目政策。

## 发布核验

区分源码提交、测试产物与公开 Release。仅以对应标签的实际校验值和验证记录为准；不继承上游的 SLSA、签名、VirusTotal 或跨平台声明。许可证和第三方声明继续保留。
