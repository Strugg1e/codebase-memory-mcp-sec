# 参与 CBM Sec 开发

先阅读 [README](README.md)、[仓库结构](docs/development/repository-layout.md)和[当前交付状态](docs/development/status.md)。本仓库保留上游组件，但安全产品的默认开发入口是 `security/` 与 `Makefile.security`，不是原版安装器。

## 提交前

在独立分支完成聚焦改动。安全算法、框架模型、文档整理和发行操作尽量分开；先核对远程基线，不能把未合入的本地候选当成当前功能。

```sh
make docs-check
make test
```

框架语义修改应增加正反例；值流改动要覆盖错误类型、覆盖赋值、分支、未知项及预算限制。能够使用受控框架参考验证时，保存实际参考输出；不运行任意被审计项目来完成普通单元测试。

## 代码与文档放哪里

| 内容 | 位置 |
|---|---|
| 事实、框架与程序关系 | `security/` |
| 安全回归、协议与失败状态 | `tests/security/` |
| 独立框架参考 | `tests/*-reference/` |
| 当前使用文档、详细接口 | `docs/`、`docs/reference/` |
| Agent 工具用法 | `skills/cbm-sec-evidence/` |
| 上游来源材料 | `docs/upstream/` |

保留源码引用和未知项，不让名称匹配成为安全证明。不要增加未验证的性能、语言深度、平台支持或漏洞召回承诺。功能变更时同步机器能力表和工具技能；纯目录调整不必升级算法版本。

## 评审和贡献声明

PR 写明基线、改动目的、实际执行的测试、未执行项目、兼容影响和已知限制。保留 [DCO](DCO) 要求。贡献者仅在自己能够作出该声明时签署 `Signed-off-by`，不替他人或自动化工具伪造认证。

不关闭现有检查、不绕过分支保护、不强制覆盖远程、不自动合并主分支或移动旧标签。只读分析程序与验证目标的受控实验保持分离。漏洞报告按 [SECURITY.md](SECURITY.md) 处理，不在公开 PR 中粘贴秘密或业务源码。
