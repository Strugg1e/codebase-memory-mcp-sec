name: Spring Security control reference
on:
  pull_request:
    paths:
      - 'security/**'
      - 'tests/security/**'
      - 'tests/spring-security-reference/**'
      - 'Makefile.security'
      - '.github/workflows/security-control-reference.yml'
permissions:
  contents: read
jobs:
  selected-controls:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          persist-credentials: false
      - name: Build CBM Sec
        run: make -f Makefile.security -j2
      - name: Build fixed test-only framework fixture
        run: |
          java -version
          mvn -B -q -f tests/spring-security-reference/pom.xml compile dependency:build-classpath \
            -Dmdep.outputFile="$PWD/tests/spring-security-reference/target/deps.classpath"
      - name: Compare chain and rule selection with framework
        run: |
          set -euo pipefail
          java -cp "tests/spring-security-reference/target/classes:$(cat tests/spring-security-reference/target/deps.classpath)" \
            reference.SecurityOracle > tests/spring-security-reference/target/reference.json
          python3 tests/spring-security-reference/compare.py build/security/cbm-security-facts \
            tests/spring-security-reference/target/reference.json | tee tests/spring-security-reference/target/comparison.json
      - name: Save reference evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: spring-security-reference-${{ github.event.pull_request.head.sha }}
          path: |
            tests/spring-security-reference/target/reference.json
            tests/spring-security-reference/target/comparison.json
            tests/spring-security-reference/target/deps.classpath
          if-no-files-found: warn
          retention-days: 7
